require 'spec_helper'

describe CelticsJerseysGem do
  it 'has a version number' do
    expect(CelticsJerseysGem::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
